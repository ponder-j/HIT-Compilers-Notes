# 编译原理期末复习模拟卷

**总分：100分 | 建议用时：120分钟** **考查重点：推导与分析树、自动机与词法分析、LL(1)/SLR(1)语法分析、中间代码生成、运行存储分配、DAG与代码优化**

### 一、 语言及其文法（15分）

已知上下文无关文法 $G(E)$ 如下：

$$E \to E + E \mid E * E \mid (E) \mid \mathbf{id}$$

1. 给出句子 $\mathbf{id} + \mathbf{id} * \mathbf{id}$ 的**最左推导**和**最右推导（规范推导）**。（6分）
2. 画出该句子对应的**语法分析树**。（4分）
3. 证明该文法是**二义性文法**。（5分）

### 二、 词法分析（20分）

已知描述某种语言单词的正规表达式为：$RE = (a \mid b)^* a b b$

1. 使用 Thompson 算法，画出该正规表达式对应的 **NFA（带有** $\varepsilon$ **转换的非确定有穷自动机）**。（6分）
2. 使用**子集构造法**将上述 NFA 确定化，列出状态转换表，并画出相应的 **DFA** 状态转换图。（8分）
3. 使用 Hopcroft 分割法对得到的 DFA 进行**最小化**，画出最小化后的 DFA 状态转换图。（6分）

### 三、 自顶向下语法分析：LL(1) 分析（15分）

已知文法 $G(S)$ 如下：

$$S \to A B$$

$$A \to a A \mid \varepsilon$$

$$B \to b B \mid c$$

1. 计算该文法中所有非终结符的 **FIRST 集合** 和 **FOLLOW 集合**。（6分）
2. 构造该文法的 **LL(1) 预测分析表**，并判断其是否为 LL(1) 文法。（6分）
3. 给出输入串 `a b c` 的预测分析过程（列出步骤、符号栈、输入串、动作）。（3分）

### 四、 自底向上语法分析：SLR(1) 分析（20分）

已知拓广文法 $G(S')$ 如下： (0) $S' \to S$ (1) $S \to S + A$ (2) $S \to A$ (3) $A \to a$

1. 构造该文法的 **LR(0) 项目集规范族**（画出识别活前缀的 DFA 图）。（8分）
2. 指出 LR(0) 分析中哪个状态存在冲突，是什么冲突？利用 FOLLOW 集合说明如何解决该冲突。（4分）
3. 构造该文法的 **SLR(1) 分析表**。（5分）
4. 给出输入串 `a + a` 的移入-归约分析过程（列出步骤、状态栈、符号栈、输入串、动作）。（3分）

### 五、 中间代码生成与回填（10分）

请将下面这段代码翻译为**四元式（三地址码）\**序列。要求使用\**回填（Backpatching）技术**的思想，对于控制流的跳转，明确写出四元式序列中的跳转目标。（假设 `x`, `y`, `a`, `b`, `z` 均为整型变量）

```
while (x < y) {
    if (a > b) {
        z = z + 1;
    }
}
```

### 六、 运行存储分配（10分）

假设有如下类 Pascal 语言编写的嵌套过程：

```
procedure Main;
  var x: integer;
  
  procedure P1;
    var y: integer;
    
    procedure P2;
      var z: integer;
    begin
      // [断点 A]
      x := y + z;
    end; // P2
    
  begin
    P2();
  end; // P1

begin
  P1();
end. // Main
```

当程序执行到 `[断点 A]` 处时：

1. 画出此时**运行时的栈帧（活动记录）结构**图。（4分）
2. 在图中标出每个栈帧的动态链（控制链）**和**静态链（访问链）的指向。（6分）

### 七、 代码优化：DAG 与基本块优化（10分）

已知某基本块中的三地址码序列如下：

1. $t_1 = a + b$
2. $t_2 = c - d$
3. $t_3 = a + b$
4. $t_4 = t_3 + t_2$
5. $t_5 = c - d$
6. $t_6 = t_4 * t_5$
7. $x = t_6$
8. 根据上述代码序列构造 **DAG（有向无环图）**。（5分）
9. 利用 DAG 对基本块进行**局部优化**（删除公共子表达式、无用赋值等），写出**优化后的三地址码序列**。（假设只有 `x` 在该基本块之后仍活跃（是活跃变量），中间变量 $t_1 \sim t_6$ 均不再使用）。（5分）

*祝你考试顺利！*